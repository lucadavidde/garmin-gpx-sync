# Virtualenv (rebuild per-machine, don't commit)
venv/

# Login session cache - contains auth tokens
.garmin_tokens/

# Downloaded/generated data - personal, not source
gpx/
state.json
status.json
sync.log

# Python
__pycache__/
*.pyc
